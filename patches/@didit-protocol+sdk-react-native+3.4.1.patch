diff --git a/node_modules/@didit-protocol/sdk-react-native/app.plugin.js b/node_modules/@didit-protocol/sdk-react-native/app.plugin.js
index 82be562..8271443 100644
--- a/node_modules/@didit-protocol/sdk-react-native/app.plugin.js
+++ b/node_modules/@didit-protocol/sdk-react-native/app.plugin.js
@@ -17,7 +17,7 @@ const MAVEN_REPO =
 const MAVEN_LINE = `        maven { url "${MAVEN_REPO}" }`;
 
 const PODSPEC_URL =
-  'https://raw.githubusercontent.com/didit-protocol/sdk-ios/main/DiditSDK.podspec';
+  'https://raw.githubusercontent.com/didit-protocol/sdk-ios/3.6.1/DiditSDK.podspec';
 
 // Key written into ios/Podfile.properties.json so the Podfile and the
 // SdkReactNative.podspec it loads can both read the same value at
@@ -195,6 +195,8 @@ function withDiditIosPodspec(config) {
         ").downcase != 'false'",
       "didit_sdk_ios_pod = didit_sdk_ios_nfc_enabled ? 'DiditSDK' : 'DiditSDK/Core'",
       `pod didit_sdk_ios_pod, :podspec => '${PODSPEC_URL}'`,
+      "pod 'GoogleUtilities', :modular_headers => true",
+      "pod 'RecaptchaInterop', :modular_headers => true",
     ].join('\n');
 
     const result = mergeContents({
